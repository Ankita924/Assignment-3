# Assignment-3

Task 1: It takes a number as input from the user and return its factorial as output. I defined a function named factorial which takes the number as arguement. Then it checks if the number is less than 2 or not. If it is less than 2 then the function returns 1 and if it is not then it returns the number multiplied by the factorial of its predecessor. This later process runs in a loop until the predecessor becomes less than 2. then I created a variable which stores the value of the function call and printed it which gives the required output.

Task 2: Here first I imported all from the math module then took a number as input from the user. Then I used the sqrt(), log(x, base) and sin() functions to calculate the square root, logarithm and sin value in radian of the respected number and produced it as output.
